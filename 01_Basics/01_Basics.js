"use strict";//to activate js strict mode(reduces some errors,exceptions and makes code run faster)
console.log('Arijit');
let num = 14.256
let accountState;
console.table([num,accountState]);
//alert('hello')// this would work in browser but not in node
console.log(typeof null )